View를 View답게
	퍼블리셔
	웹 디자이너
 == JSP페이지내에서 Java코드의 개입을 줄여나가자

 1) EL 표현언어
 2) 커스텀태그
 3) JSTL

<jsp:useBean id="test" class>
	Test test=new Test()
<%=test.getNum()%>
 1. 가독성 2. java코드가 개입되어있음
${test.num}  ${test["num"]}
${test[0]} == test라는 컬렉션객체가 있었는데, 거기에서 [0]에 존재하는 값
산술, 비교, 논리연산자를 가짐


커스텀태그
 화면의 코드를 단순하게 변경가능
 로직등을 화면에서 숨길수있음 == 캡슐화
	-> 보다 완벽한 MVC패턴
 1) 태그 파일기반 
	비교적 구현이 간단
 2) 태그 핸들러클래스기반(.java)
	배포가 용이
	커스텀태그 정의파일 필요함!
	(xml파일 == 설정파일)




