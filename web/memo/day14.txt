MVC 설계패턴으로 프로그램을 제작
M
 DB와 연동(JDBC) 
  + 웹 크롤링을 통한 초기데이터를 설정
V 
 반응형 웹 템플릿에 데이터를 사용자에게 제공
C
 사용자의 요청흐름을 파악하고 데이터를 제공

1. 설계★★★★★
 -개발환경 -개발기간 -버전관리 -기획목적
 -느낀점 -역할소개 
 -ERD(M) -User Flow(V) -CRUD(비즈니스메서드)의 사용흐름==Logic Process(C)
 -장점어필
 => 간단하게 문서화하셔서(.txt) 피드백통과해야 코딩!
 
2. 검색★
 -벤치마킹한 어플리케이션에대한 분석 보고서
 -웹 크롤링 포트폴리오


------------------------------------------------------------

게시판 프로그램

Model파트
[게시글] 테이블
☆pk 글번호 -> 사용자가 조작할수없는 데이터
- 사용자명
- 글제목
글내용(상세페이지)

M
  VO
  CRUD => DAO
  연동방식 : JDBC / DBMS
	테이블  명세할때, 각 칼럼의 제약조건을 설정
1) 테이블 명세(기술)
2) VO
3) DAO -> 5개 + 검색6~7 10000~20000 "필터검색"
				where ?<=price and price <=?
=> 내가 개발한 DAO의 CRUD메서드는 다음과 같고, 
 C메서드는 인자가 작성자,제목,내용을 받아서 DB에 insert
	pk라서 자동으로 +1처리해줄거야.
 R메서드_select_one은 pk를 받아서 상세페이지 데이터(VO)를 return해준다.
 각 메서드에대해 어떤 인자가 필요한지 
M -- C -- V

V 
  index.jsp controller.jsp등을 제외한,
  사용자에게 데이터나 화면을 제공해주는 .jsp페이지를 관리
  브라우저에 출력되는 페이지(UI) 제공
=> 본인의 작업을 시작하기전에, C에게 물어보러감
	필요한 데이터(name설정 올바르게할것!) / 보여줘야하는 데이터 ???
	action파라미터로 수행값을 전달

☆상세페이지를 보여줄때 idx가 필요함
☆AL<BoardVO> 컬렉션데이터를 보여줘야함
	- idx -content(눌러야 보이기 때문에 가려도 상관없음) -uname -title

☆BoardVO를 보여줘야함
	-uname -title -content
☆수정, 변경처리를 처리해야함
	-idx -uname -title -content	


Java코드의 개입을 최소화==EL,JSTL,커스텀태그


C
  index.jsp controller.jsp
  V가 필요한 사항들을 물어봄 --> M한테 확인
  controller.jsp는 결국 .java파일이된다!!!!!★
	어떤 V페이지에 어떤 M 데이터가 필요한지 파악완료된 상태!!!


※과제※
상세페이지에 출력할 데이터가 없을때,
 1- 목록을 보여주기(main.jsp)
 2- 출력할 데이터가 없습니다!(board.jsp)
	로직 if문 JAVA코드 -->> JSTL
※월요일에 풀이




















